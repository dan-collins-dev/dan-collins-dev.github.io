---
import HomeLayout from "./HomeLayout.astro";
const {frontmatter} = Astro.props;
---
<HomeLayout pageTitle="FUC">

    <h1>{frontmatter.title}</h1>
    <p>Written by {frontmatter.author}</p>
    <p>Date Published: {frontmatter.datePublished}</p>
    <slot />
</HomeLayout>

